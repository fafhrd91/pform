<textarea id="{{context.id}}" name="{{context.name}}"
       class="{{context.klass}}" title="{{context.title}}" value="{{value}}"
       {{ {'lang': context.lang, 'disabled': context.disabled,
           'tabindex': context.tabindex, 'readonly': context.readonly,
           'cols': context.cols, 'rows': context.rows,
           'alt':  context.alt, 'accesskey': context.accesskey,
           'size': context.size, 'maxlength': context.maxlength}|xmlattr}}>{{value}}</textarea>
